---
# Source: helm for kubernetes/templates/ingress.yaml
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: popo                  # Ingress name
  labels:
    app: popo
spec:
  ingressClassName: nginx   # Optional ingressClass
  rules:
    - host: "popo.smartops.space"
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: popo               # Links to Service name dynamically
                port:
                  number: 8080  # Links to service port dynamically
